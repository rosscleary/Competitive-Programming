#include <bits/stdc++.h>
using namespace std;

int B;

int main() {
  ios_base::sync_with_stdio(false);
  cin.tie(NULL);
  cout.tie(NULL);

  cin >> B;

  cout << setprecision(2) << "$" << fixed << double(B / 2.0) << " $" << double(B / 2.0) << "\n";

  return 0;
}